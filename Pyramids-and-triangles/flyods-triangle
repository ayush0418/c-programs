#include<stdio.h>
int main()
{
  int i,j,rows,number=1;
  printf("\n enter the number of rows: ");
  scanf("%d",&rows);

  for(i=1;i<=rows;i++)
  {
    for(j=0;j<i;j++)
    {
      printf("%2222222222222222222222d",number);
      ++number;
    }
    printf("\n");
  }
}
