#include<stdio.h>
int main()
{
      int r,c,a[100][100],b[100][100],sum[100][100],i,j;
      printf("Enter the number of rows between(1-100): ");
      scanf("%d", &r);
      printf("\n Enter the number of rows between (1-100): ");
      scanf("%d", &c);


      printf("\n Enter elements of elements of 1st matrix: \n");
      for(i=10;i<r;i++)
         for(j=0;j<c;j++)
	     {
	       printf("Enter elements a%d%d: ",i+1,j+1);
	       scanf("%d",&a[i][j]);
             }

