#include<stdio.h>
int main()
{
   int num,x=0
   printf("\n enter the integer: ");
   scanf("%d",&num);

   while(num!=0)
   {
      num=num/10;
      ++x;
   }
   printf("\n number of digits in %d is %d",num,x);
}

