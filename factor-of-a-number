#include<stdio.h>
int main()
{
    int i,num;
    printf("\n enter the integer: ");
    scanf("%d",&num);
    printf("\n Factors of %d are:"num);

    for(i=1;i<=num;i++)
    {
      if(num%i==0)
      {
         printf(" %d",i)
      }
    }
}

